<style>
    .Toast {
        font-family: sans-serif;
        position: absolute;
        right: 0;
        border-top-left-radius: 8px;
        border-bottom-left-radius: 8px;
        border-width: 1px 0px 1px 1px;
        border-style: solid;
        overflow: auto;
        min-width: 200px;
    }

    .ToastHeader {
        color: #fff;
        padding-bottom: 5px;
    }

    .ToastIcon {
        padding: 0 10px;
        font-size: 1.5em;
    }

    .ToastTitle {
        font-size: 0.9em;
        font-weight: bold;
        vertical-align: super;
        position: absolute;
        top: 9px;
    }

    .ToastTimesince {
        opacity: 0.7;
        font-size: 0.5em;
        float: right;
        margin: 12px;
    }

    .ToastBody {
        font-size: 0.7em;
        padding: 10px;
    }
</style>
<div class="Toast Toast{{upper level}}" style="border-color:hsl({{hue}},67%,50%);bottom:{{bottom}}px;">
    <div class="ToastHeader" style="background-color:hsl({{hue}},67%,50%)">
        <span class="ToastIcon">&#x26a0;</span>
        <span class="ToastTitle">{{title}}</span>
        <span class="ToastTimesince">{{timesince}}</span>
    </div>
    <div class="ToastBody" style="color:hsl({{hue}},67%,50%)">{{body}}</div>
</div>
